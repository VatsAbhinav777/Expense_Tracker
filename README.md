# Expense_Tracker
A Python-based console application for tracking personal finances efficiently. Users can add expenses with descriptions or remove entries via a simple menu. The program ensures data persistence by automatically loading from and saving to a local file during every session.
